//! `propagon` CLI — fleshed out in stage S6; this stub keeps the workspace
//! member valid while the core library lands.

fn main() {
    eprintln!("propagon v2 CLI is under construction (see docs/PRD.md)");
    std::process::exit(2);
}
