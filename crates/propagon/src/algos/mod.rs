//! Algorithm implementations.
//!
//! Each module pairs an algorithm struct (its public fields are the params,
//! `Default` gives sensible values) with an owned, serializable model type.
//! See `docs/algorithms.md` for the survey entry behind each method.

// Populated stage by stage (S2-S4); intentionally empty at the foundation
// commit so the crate builds and the dataset/trait layer is testable.
